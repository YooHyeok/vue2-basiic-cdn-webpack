// import Vue from 'vue'
import { createApp } from 'vue';
import LottoGenerator from './LottoGenerator'

// new Vue(RockScissorsPapper).$mount('#root'); // vue2 마운트
createApp(LottoGenerator).mount('#root'); // vue3 마운트