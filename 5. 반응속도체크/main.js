import Vue from 'vue'
import { createApp } from 'vue';
import ResponseCheck from './ResponseCheck'

// new Vue(ResponseCheck).$mount('#root'); // vue2 마운트
createApp(ResponseCheck).mount('#root'); // vue3 마운트