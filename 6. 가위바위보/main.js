// import Vue from 'vue'
import { createApp } from 'vue';
import RockScissorsPapper from './RockScissorsPapper'

// new Vue(RockScissorsPapper).$mount('#root'); // vue2 마운트
createApp(RockScissorsPapper).mount('#root'); // vue3 마운트